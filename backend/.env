# --- CONFIGURACIÓN DE BASE DE DATOS LOCAL ---
# Conexión local estándar de MongoDB (para MongoDB Compass)
MONGO_URI=mongodb://localhost:27017/energisense_db
PORT=5000

# --- CONFIGURACIÓN DE AUTENTICACIÓN JWT ---
# Clave secreta para firmar los tokens de autenticación (IMPORTANTE: cámbiala por una cadena larga real)
JWT_SECRET=una_clave_secreta_muy_larga_y_segura_para_jwt